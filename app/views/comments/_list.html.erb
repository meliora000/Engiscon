
<ul id="comments_list_<%=@post.id%>_<%= @suboption %>">
  <% if @post.comments.size > 0 %>
      <%= render @post.comments %>
  <% else %>
      <li>
        No comments
      </li>
  <% end %>
</ul>